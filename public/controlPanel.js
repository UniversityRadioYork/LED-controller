document.addEventListener("DOMContentLoaded", function (event) {
	let element = document.getElementById('mode');
	element.value = set_mode;
	element = document.getElementById('palette');
	element.value = set_palette;
});